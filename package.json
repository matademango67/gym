{
  "name": "gym-root",
  "private": true,
  "scripts": {
    "test": "pnpm --dir backend test",
    "dev": "concurrently \"pnpm --dir backend dev\" \"pnpm --dir frontend dev\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}